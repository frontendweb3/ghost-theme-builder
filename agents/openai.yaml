interface:
  display_name: "Ghost Theme Builder"
  short_description: "Build and optimize Ghost CMS themes."
  default_prompt: "Use $ghost-theme-builder to build or improve a production-ready Ghost CMS theme."
